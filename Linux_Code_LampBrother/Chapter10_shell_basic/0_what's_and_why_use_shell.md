1) shell 是命令解释器
2）shell 可以作为脚本语言
3）shell 可以直接调用系统命令，这是对其他一切语言的优势
   所以对系统管理员最方便
   
Bourne Shell   .sh
bash 
csh             bsd 
zcsh            
zsh  

# sudo cp .bashrc /root
sudo cat /etc/shells

/bin/sh
/bin/bash
/usr/bin/sh
/usr/bin/bash

sh  # go to Boune Shell
exit # return bash

# uid from 1000 ==> gao